import React from 'react';
import "../CSS/App.css";

const Bottom = () => {

    return (
        <div className='sub_container'>
            <h4>Bottom</h4>
            <button onClick={''}>증가</button>
        </div>
    );
};

export default Bottom;